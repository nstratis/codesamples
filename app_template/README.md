# Application HTML/CSS Template
This is the plain template used in the sample application utilizing SASS to compile,
optionally could have used LESS, PostCSS, or added imported CSS files per framework
component.
